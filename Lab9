import math
defg(x):
   return math.log(x)+20
x0=3.0
eps=1e-6
max_inter=100
iteration=0
while interation <max_iter:
  x1=g(x0)
  if f(c)==0:
     break
  elif f(a)*f(c)<0:
      b=c
   else:
     a=c
print(f"Təqribi kök : {c}")
