import math

def f(x):
    return math.exp(x) - math.log(x) - 20

a = 3.0
b = 3.2
eps = 1e-6 

while (b - a) / 2 > eps:
    c = (a + b) / 2
    if f(c) == 0:
        break
    elif f(a) * f(c) < 0:
        b = c
    else:
        a = c

print(f"Təqribi kök: {c}")
